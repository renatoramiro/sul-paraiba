<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :categoria_id %><br />
    <%= f.collection_select :categoria_id, Categoria.all(:order => :nome), :id, :nome, {:prompt => true} %>
  </div>
  <div class="field">
    <%= f.label :autor %><br />
    <%= f.text_field :autor %>
  </div>
  <div class="field">
    <%= f.label 'Título' %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :texto %><br />
    <%= f.cktext_area :texto, :toolbar=>'Full', :width=>'800px', :height=>'200px' %>
  </div>  
  <div class="actions">
    <%= f.submit 'Enviar' %>
  </div>
<% end %>
