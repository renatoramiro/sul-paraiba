<%= form_for(@noticia, :html => {:multipart => true}) do |f| %>
  <% if @noticia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noticia.errors.count, "error") %> prohibited this noticia from being saved:</h2>

      <ul>
      <% @noticia.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Título" %><br />
    <%= f.text_field :titulo %>
  </div>  
  <div class="field">
    <%= f.label :texto %><br />
    <%= f.cktext_area :texto, :toolbar=>'Full', :width=>'800px', :height=>'200px' %>
  </div>
  <div class="field">
    <%= f.label :autor %><br />
    <%= f.text_field :autor %>
  </div>
  <div class="field">
    <%= f.label "Material de divulgação:" %>
    <%= f.file_field :material_divulgacao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
